import Login from "./components/Login";

export default function LoginPage() {
  return (
    <main className="w-full bg-transparent flex-1 flex flex-col overflow-hidden">
      <Login />
    </main>
  );
}