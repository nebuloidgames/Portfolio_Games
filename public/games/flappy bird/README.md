# FlappyVerse Premium Event Game

React + Tailwind CSS + Vite.

Put the event logo at `public/logo.png`.

Run:
npm install
npm run dev

Flow:
Start Game -> Enter Name -> 10 Levels -> Certificate

Controls:
Tap / click / Space / Arrow Up = flap
R = retry after crash
